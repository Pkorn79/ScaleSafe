interface:
  display_name: "Web Builder"
  short_description: "Build and publish polished business websites"
  default_prompt: "Use $web-builder to build a polished, responsive business website and verify it before publishing."

policy:
  allow_implicit_invocation: true
