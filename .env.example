# ============================================
# ScaleSafe Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to the repo
# ============================================

# GHL Marketplace App (from GHL Developer Portal)
GHL_APP_CLIENT_ID=
GHL_APP_CLIENT_SECRET=
GHL_APP_SSO_KEY=
GHL_API_DOMAIN=https://services.leadconnectorhq.com

# Optional overrides. The current HighLevel plan IDs are built into the app;
# set these only if HighLevel replaces a Marketplace plan.
GHL_MARKETPLACE_TEST_PLAN_ID=
GHL_MARKETPLACE_STANDARD_PLAN_ID=
GHL_MARKETPLACE_WHOLEPAY_PLAN_ID=

# Stripe Connect platform credentials. Set all five values together.
# Use false for a test platform and true for the production live platform.
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_CLIENT_ID=
STRIPE_WEBHOOK_SECRET=
STRIPE_LIVE_MODE=false

# Supabase (from Supabase project settings)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
# Public/publishable key used only for server-side Command Center authentication.
# Prefer the newer publishable key; SUPABASE_ANON_KEY remains a compatibility fallback.
SUPABASE_PUBLISHABLE_KEY=

# Claude API (for AI Defense Compiler)
ANTHROPIC_API_KEY=

# Server
PORT=3000
NODE_ENV=development
LOG_LEVEL=debug

# Optional: enables /api/debug/* routes when supplied by trusted admin callers
DEBUG_ADMIN_TOKEN=

# Internal operator-only ScaleSafe HQ console.
SCALESAFE_HQ_ADMIN_TOKEN=

# ScaleSafe Command Center. All features default off and must be enabled in the
# documented migration-first rollout order. Use a dedicated hostname and a new
# encryption key that is different from PROCESSOR_ENCRYPTION_KEY.
OPERATOR_COMMAND_CENTER_ENABLED=false
OPERATOR_AUTH_ENABLED=false
OPERATOR_HEALTH_INCIDENTS_ENABLED=false
OPERATOR_HOST=ops.scalesafe.app
OPERATOR_TRUST_PROXY_HOPS=
OPERATOR_AUTH_TOKEN_ENCRYPTION_KEY=
OPERATOR_AUTH_ATTEMPT_MINUTES=10
OPERATOR_SESSION_IDLE_MINUTES=30
OPERATOR_SESSION_ABSOLUTE_MINUTES=720
OPERATOR_INVITATION_HOURS=72

# Signed Guardian ingestion. Keep disabled until migrations 107-110, the owner
# Command Center login, and the dedicated Guardian host are certified live.
GUARDIAN_INGESTION_ENABLED=false
GUARDIAN_HOST=guardian.scalesafe.app
GUARDIAN_MAX_BODY_BYTES=65536
GUARDIAN_TIMESTAMP_TOLERANCE_SECONDS=300
GUARDIAN_BUILD_SHA_ENV=RAILWAY_GIT_COMMIT_SHA
APP_VERSION=1.0.0

# Global emergency switch for third-party evidence enrollment binding. Provider
# and sub-account rollout is stored in Supabase after migration 092.
EVIDENCE_CONNECTOR_AUTOMATION_ENABLED=false

# Local-only shortcut for tests/dev tools that send x-location-id without GHL SSO
ALLOW_DEV_LOCATION_AUTH=false

# Optional: separate HMAC secret for signed client action links.
# Falls back to GHL_APP_SSO_KEY when unset.
PUBLIC_ACTION_TOKEN_SECRET=

# Emergency compatibility only: allow old public links with raw contactId/locationId in production.
ALLOW_LEGACY_PUBLIC_ACTION_LINKS=false

# Emergency compatibility only: allow official GHL marketplace webhooks without signature in production.
ALLOW_UNSIGNED_GHL_WEBHOOKS=false

# Set this same random value as an x-scalesafe-trigger-secret header on every
# GHL Marketplace custom-trigger Subscription URL.
GHL_TRIGGER_SUBSCRIPTION_SECRET=

# Rollout flag for GHL workflow Custom Webhook shared secrets.
# Production always rejects missing/invalid x-scalesafe-webhook-secret headers.
# Set true in non-production when you want to test strict webhook enforcement locally.
REQUIRE_WEBHOOK_SECRET=false

# UI-only testing flag: shows Daily (Testing) as an offer billing frequency.
# Use only in sandbox/test builds for recurring billing verification.
VITE_ENABLE_DAILY_TEST_BILLING=false
